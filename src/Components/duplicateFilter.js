//If the call sign or the aircraftRegistration and the departureTime are the same, then it is a duplicate

// function duplicateFilter(flight) {
  
//         return (
//           t.callSign === item.callSign &&
//           t.aircraftRegistration === item.aircraftRegistration &&
//           t.departureTime === item.departureTime
//         );
//       }) === index
//   return unique;
// }

// export default duplicateFilter;
